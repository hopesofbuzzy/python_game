# Архитектура проекта
## Глобальные решения
* EventBus
* Observer/Events модель
* MVC model для игровых объектов и глобальной архитектуры

## Game Loop Pipeline
Стандартный конвейер кадра.
1. INPUT  → Сбор клавиш/мыши → преобразование в команды
2. UPDATE → Обновление логики (физика, ИИ, таймеры, спавн, урон)
3. SYNC   → Копирование состояния Model → View (только если MVP)
4. DRAW   → Отрисовка всего на поверхности
5. FLIP   → Вывод на экран + ограничение FPS + расчёт dt для следующего кадра

## Правильный порядок
1. Input       - обработка ввода (клавиши, мышь)
2. AI          - решения ИИ (FSM, BT, minimax)
3. Movement    - применение скоростей, сил
4. Collision   - обнаружение столкновений
5. Resolution  - разрешение столкновений (отталкивание, урон)
6. Game Logic  - проверка условий (смерть, победа, триггеры)
7. Animation   - обновление кадров анимации
8. Cleanup     - удаление мёртвых сущностей

# MVC
Controller отслеживает ввод пользователя и сообщает изменения модели.
Model работает с данными.
View на основе модели отображает по своим правилам объект.

## Game
Основной объект игры внутри game-loop.

## Сцены
Сцена - контейнер для текущего состояния мира, содержащий сущности.

Важно: чтобы сцена не стала GodObject, используем разделение ответсвтенности. Сцена только
вызывает системы в необходимом порядке, но не знает реализации.

## Системы типа физики и коллизии (разделение ответственности)
1. EntityManager отдаёт список [ICollidable]

2. PhysicsSystem.update(dt, collidables)
   1. только читает .get_collision_rect() и .get_collision_layer()
   2. при пересечении публикует EVENT_COLLISION_DETECTED
3. EventBus
   1. рассылает слушателям
4. PlayerModel / TrapSystem / DamageSystem
   1. реагируют на событие (меняют HP, отбрасывают, запускают анимацию)

## ECS или ООП
В случае с MVP/MVC ООП начинает ломаться, НО ООП чище с точки зрения архитектурной инкапсуляции.
А ещё ECS может быть overengineering.